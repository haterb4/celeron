// #!/usr/bin/env node
// import { Command } from 'commander';
// import { version } from '../package.json';

// const program = new Command();

// program
//   .version(version)
//   .name('celeron')
//   .description('Celeron framework CLI');

// program
//   .command('new')
//   .description('Create a new Celeron project')
//   .argument('<name>', 'Project name')
//   .action((name) => {
//     console.log(`Creating new project: ${name}`);
// });

// program.parse();

// @types/node @typescript-eslint/eslint-plugin @typescript-eslint/parser eslint husky jest lerna prettier ts-jest typescript ts-node-dev